HA，高可用，high availale 
1.有节点宕机的时候，通过多个provider集群+注册中心，可以运行期保障服务整体可用，【OK，通过注册中心+多服务端】
2.有节点偶尔异常、但是没有宕机，我们可以通过重试+LB重新选择节点，实现这次调用的成功。【OK，通过重试】
3.有节点在一段时间内异常（该节点可能提供多个接口，但只有部分有问题），没有宕机，其他服务正常，【TODO，故障隔离】
4.对于已经故障隔离的节点，进行定时探活
    - 探活好了，就可以做故障恢复
    - 每次定时探活，我们放一笔流量进来，半开，half open

